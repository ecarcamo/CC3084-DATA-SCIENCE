# Laboratorio 6 — Análisis de redes sociales (YouTube)
# Python 3.12 · instalar con:  pip install -r requirements.txt

pandas>=2.2
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13

# Texto (Sec. 2 y 9)
nltk>=3.9                      # stopwords en español (nltk.download('stopwords'))
wordcloud>=1.9
emoji>=2.12
spacy>=3.7
# modelo de lematización en español (instalar aparte):
#   python -m spacy download es_core_news_sm
es-core-news-sm @ https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.8.0/es_core_news_sm-3.8.0-py3-none-any.whl

# Redes (Sec. 4-8)
networkx>=3.3
scipy>=1.11                    # requerido por networkx para pagerank / eigenvector (Etapa 8)
python-louvain>=0.16          # import community  ->  detección de comunidades (Louvain)

# Notebooks
jupyter>=1.0
ipykernel>=6.29
jupytext>=1.16                # los notebooks se pueden editar/versionar como .py (formato percent)

# Sentimiento en español (Sec. 9) — pesado (torch); descomentar cuando se llegue a la Etapa 9
# pysentimiento>=0.7
